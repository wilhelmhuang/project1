name: After PR is merged

on:
  pull_request:
    branches:
      - master
    types:
      - closed


jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: check out repo
        uses: actions/checkout@main
      - name: read commits
        run: |
          echo 'COMMITS<<EOF' >> $GITHUB_ENV
          git log 22801b4..HEAD --oneline >> $GITHUB_ENV
          echo 'hehe' >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV
      - name: print env var
        run: |
          echo "${{ env.COMMITS }}"